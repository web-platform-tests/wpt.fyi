on: push
name: Test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ./
        name: Docker
        with:
          entrypoint: /bin/bash
          args: -c "pwd; ls -a"

