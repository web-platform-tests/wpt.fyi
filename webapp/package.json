{
  "name": "wptdashboard",
  "devDependencies": {
    "babel-eslint": "^8.2.3",
    "eslint": "^4.19.1",
    "eslint-plugin-html": "^3.2.2",
    "web-component-tester": "^6.4.3"
  },
  "scripts": {
    "test": "npm run lint && npm run wct",
    "lint": "eslint --plugin html components/*.html test/*.html",
    "lint-fix": "eslint --plugin html components/*.html test/*.html --fix",
    "wctp": "wct -p",
    "wct": "wct"
  }
}
